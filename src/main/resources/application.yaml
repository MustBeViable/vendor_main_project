spring:
  application:
    name: vendor_main

  datasource:
    url: jdbc:mariadb://localhost:3306/test_database
    username: root
    password:
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8081