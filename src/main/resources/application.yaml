spring:
  application:
    name: vendor_main

  datasource:
    url: jdbc:mariadb://localhost:3306/${DB_NAME}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8081