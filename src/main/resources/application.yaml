spring:
  application:
    name: vendor_main

  datasource:
    url: jdbc:mariadb://localhost:3306/store_app
    username: db_admin
    password: secret_password
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8081